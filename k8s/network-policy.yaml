apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: hogfm-api-ingress
spec:
  podSelector:
    matchLabels:
      app: hogfm-api
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector: {}
      ports:
        - protocol: TCP
          port: 8000
